declare type Props = {
    text?: string;
    url?: string;
    hashtags?: string;
    via?: string;
    related?: string;
    replyTo?: string;
};
export declare const shareTwitterURL: ({ text, url, hashtags, via, related, replyTo }: Props) => string;
export {};
